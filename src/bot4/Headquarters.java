package bot4;

import battlecode.common.*;

import bot4.util.*;

public class Headquarters {

	/**
	 * Responsibilities:
	 * - Choose a new mission from Planning and pass it to Communication 
	 * - Retrieve map info from Communication and pass it to Mapping */
	static void run(RobotController rc) throws GameActionException {
		//Mission m1 = Plan.chooseMission(rc);
		//Communication.writeMission(m1);

		//// TODO how are we planning to distinguish new missions?
		//Mission m2 = Communication.readMission(); 
		//Mapping.addMission(m2);
	}

}
